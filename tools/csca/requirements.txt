# Versions ÉPINGLÉES À LA DERNIÈRE VERSION : l'épinglage sert la REPRODUCTIBILITÉ
# (générer le magasin deux fois avec les mêmes sources doit donner le même résultat,
# quel que soit le poste ou le runner) — pas à figer de vieilles versions.
#
# Mettre à jour librement : le repli openssl de update_csca.py garde les CSCA à
# l'encodage laxiste (sérials DER négatifs, paramètres NULL ECDSA) que cryptography
# rejette depuis la v43, avec une sortie identique bit à bit (vérifié sur 42.0.0,
# 42.0.8 et 49.0.0). Si un futur bump changeait quand même la sortie, le garde
# « mêmes éditions, même magasin » du workflow échoue bruyamment au lieu d'ouvrir
# une PR trompeuse.
cryptography==49.0.0
requests==2.34.2
